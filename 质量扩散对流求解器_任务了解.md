# 质量扩散对流方程求解器 —— 任务了解文档

> 状态：2026-09-02 整理。任务原话未留存档案；本文档依据沟通记忆与前序任务（FEALPy Poisson 求解器）重建，带【待确认】标记的点尚未最终核实。

## 0. 一句话任务

把 Poisson 求解器推广为「质量扩散对流」（**对流-扩散-反应**方程）**一般方程**的求解器：方程三个项原来系数都是 1，现在换成**多项式**系数函数 $a(x)$、$b(x)$、$c(x)$；要求交付：**程序 + 设计方案 + 数值算例 + 数值正确性验证 + 图片直观展示**。

## 1. 方程背景

### 1.1 一维形式

$$-(a(x)u')' + b(x)u' + c(x)u = f(x), \qquad x\in(0,1), \qquad u(0)=u_0,\ u(1)=u_1$$

左边三个项一一对应三个系数函数：

| 项 | 形式 | 物理含义 | 系数条件 |
|---|---|---|---|
| 扩散项 | $-(a(x)u')'$ | 扩散（热量、污染物向四周散开） | $a(x)>0$ |
| 对流项 | $b(x)u'$ | 对流（风/水流把物质往下游搬运） | 任意 |
| 反应/质量项 | $c(x)u$ | 反应或质量变化（污染物衰减、吸收） | $c(x)\ge 0$ |

基准方程（原系数全为 1）：$-u''+u'+u=f$，即 $a=b=c=1$。

### 1.2 二维推广

$$-\nabla\cdot(a(x)\nabla u) + \boldsymbol{b}(x)\cdot\nabla u + c(x)u = f(x), \qquad x\in\Omega\subset\mathbb{R}^2$$

### 1.3 弱形式（求解器的数学核心）

方程两边乘检验函数 $v\in H_0^1$，分部积分得：

$$\int_\Omega \Big(a\,\nabla u\cdot\nabla v + (\boldsymbol b\cdot\nabla u)\,v + c\,uv\Big)\,dx = \int_\Omega f\,v\,dx$$

离散后三项对应三块矩阵，总矩阵 $\mathbf A = \mathbf K + \mathbf B + \mathbf M$：

- 扩散项 → 刚度矩阵 $\mathbf K$（**对称**正定）
- 对流项 → $\mathbf B$（**非对称**——与 Poisson 的第一个新差异，`spsolve` 直接解没问题）
- 反应项 → 质量矩阵 $\mathbf M$（对称）

### 1.4 与 Poisson 的关系

取 $a=1,\ b=0,\ c=0$ 时方程退化回 Poisson 方程 $-\Delta u=f$——所以 Poisson 只是本任务的特例，算例中应保留一组退化对照，验证新求解器与 Poisson 求解器的兼容性。

### 1.5 适定性

$a(x)>0$（椭圆性）、$c(x)\ge 0$ 时方程有唯一解。设计算例时应满足（或至少大部分区域满足）这两个条件。

## 2. 任务要求（5 项交付物）

| # | 交付物 | 内容要点 |
|---|---|---|
| 1 | 求解器程序 | FEALPy 实现；输入 a, b, c 多项式 + f + 网格 + 元次数 p；输出数值解 |
| 2 | 设计方案 | 方程与适定性 → 弱形式 → Galerkin 离散 → 系数在积分点求值 → 组装 → 边界条件 → 求解 → 验证方案 |
| 3 | 数值算例 | 2~3 组不同多项式系数（如 $a=1+x^2$、$b=x$、$c=1+x$），加一组退化 Poisson 对照 |
| 4 | 数值正确性验证 | 制造解（MMS）：给定精确解 $u$，反算 $f=-(au')'+bu'+cu$，解回来比对；L2/H1 误差表 + **收敛阶**（P1 期望 L2≈2、H1≈1；P2 期望 ≈3/≈2） |
| 5 | 图片直观展示 | 数值解 vs 精确解对比图、误差分布图、log-log 收敛阶图、三个系数函数图 |

## 3. 初步技术路线（FEALPy）

```python
# 组装：三个积分器直接对应三个项
from fealpy.fem import (BilinearForm, LinearForm, ScalarDiffusionIntegrator,
                        ScalarConvectionIntegrator, ScalarMassIntegrator,
                        ScalarSourceIntegrator, DirichletBC)

bform = BilinearForm(space)
bform.add_integrator(ScalarDiffusionIntegrator(coef=a))   # 扩散项
bform.add_integrator(ScalarConvectionIntegrator(coef=b))  # 对流项
bform.add_integrator(ScalarMassIntegrator(coef=c))        # 反应项
A = bform.assembly()

lform = LinearForm(space)
lform.add_integrator(ScalarSourceIntegrator(f))
F = lform.assembly()

A, F = DirichletBC(space, gd=u_exact).apply(A, F)
uh[:] = spsolve(A.to_scipy(), F)   # 误差与收敛阶：mesh.error
```

验证链条：制造解 $u$ → sympy 推导 $f$ 与边界值 → FEALPy 求解 → `mesh.error` 算 L2/H1 误差 → 多级网格加密 → log-log 拟合收敛阶 → 与理论阶比对。

## 4. 【待确认】问题清单

1.  **稳态还是含时间项？** 记忆中的 3 项恰好是稳态方程；若含 $\partial u/\partial t$ 则多一层时间离散（FEALPy 有 `ParabolicSTFEMModel` 可参考）。
2. **边界条件类型？** 大概率全 Dirichlet，也可能有 Neumann/Robin。
3. 系数多项式是否允许跨零（如 $a(x)$ 取负破坏椭圆性）？【默认不允许】

## 5. 推进计划

- [x] 任务了解文档（本文档）
- [ ] 调研报告：同类软件/开源项目、可行性分析（见 质量扩散对流求解器_调研报告.md）
- [ ] 设计方案文档
- [ ] 求解器程序（1D 主体）
- [ ] 数值算例 + 正确性验证（收敛阶）
- [ ] 图片展示 + 文档整合
