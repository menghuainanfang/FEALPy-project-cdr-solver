# 一维求解器入门说明：a、b、c 和 f 都在哪里

> 更新日期：2026-09-07

本次按照明确选定的方程更新代码与说明。当前主接口是 solve_cdr。过去的守恒接口只用于数学对照，不是默认算例求解入口。文档整理完成不代表你已经掌握；可以从任何一个公式继续提问。

## 1. 完整方程与符号含义

完整方程是

$$\boxed{-(a(x)u'(x))'+b(x)u'(x)+c(x)u(x)=f(x),\quad 0<x<1.}$$

还需要边界条件，例如 $u(0)=1,u(1)=2$。


| 符号 | 它是什么 | 在程序中 |
| --- | --- | --- |
| u | 要求出的未知函数，可以想成浓度曲线 | 返回的 uh 是它的数值近似 |
| a(x) | 扩散系数，决定扩散能力 | a 参数 |
| b(x) | 对流系数，影响沿空间搬运的作用 | b 参数 |
| c(x) | 零阶反应系数，c>0 通常表示吸收作用 | c 参数 |
| f(x) | 已知源项，可表示供给或移除 | f 参数，单独传入 |
| g₀、g₁ | 已知端点函数值 | gd 函数在两端的值 |

所以求解器的输入是 **a、b、c、f，加上边界和网格**。a、b、c 可以是常数，也可以是多项式函数；f 不要求必须是多项式。若原题恰好是 f=0，那叫齐次方程，是一般求解器的一个特例。

若给 f 再乘一个已知系数 d(x)，可以把 d(x)f(x) 整体视作新的源项。是否需要额外参数取决于具体模型，目前没有必要。

注意这里是 $-(au')'$，不是简单的 $-au''$。乘积求导得到

$$-(au')'=-au''-a'u'.$$

扩散系数变化时不能漏掉 $-a'u'$。我们当前实现和验证的方程已经确定，不再靠名称猜测。

## 2. 先用一个没有对流反应的小例子理解

取 a=1,b=c=0，方程变成 $-u''=f$。端点都设为 0。

- f=0 时，解是 u=0。
- f=2 时，解是 $u=x(1-x)$，中点等于 0.25。

系数和边界完全一样，只改变 f，答案就变了。这说明 f 不是可省略的装饰，也不是自动由 a、b、c 决定的。

这一对题已经加入程序独立验证。程序确实把源项组装成右端向量 F，再解 AU=F。

## 3. 电脑怎样表示一条曲线

把 [0,1] 分成 n 个小段，叫单元。P1 方法在每个小段用直线近似曲线；P2 用二次多项式。

两个单元时节点是 0、0.5、1。设它们的数值为 U₀、U₁、U₂，就能连成一条折线。写成公式：

$$u_h(x)=\sum_j U_j\phi_j(x).$$

$\phi_j$ 是帽子形基函数：在自己的节点为 1，在其他节点为 0。$U_j$ 是电脑需要解出的数字，$u_h$ 则是用这些数字拼出的函数。

P1 有 n+1 个自由度；P2 有 2n+1 个，其中两个端点值已经给定。

![帽子函数和两单元近似](./results/learning_basics.png)

右图显示：节点值可以恰好正确，但节点之间的折线仍不等于精确曲线。

## 4. 为什么要变成积分形式

折线在节点处拐弯，二阶导数不好直接处理。有限元通过分部积分，把未知函数所需的导数降到一阶。

取两端为零的检验函数 v，乘方程后积分：

$$-\int_0^1(au')'v+\int_0^1bu'v+\int_0^1cuv=\int_0^1fv.$$

只对扩散项分部积分：

$$-\int_0^1(au')'v=-[au'v]_0^1+\int_0^1au'v'.$$

v 两端为零，所以边界项消失：

$$\boxed{\int_0^1au'v'+\int_0^1bu'v+\int_0^1cuv=\int_0^1fv.}$$

这叫弱形式。注意对流项仍是正号的 bu′v。这次不使用过去守恒形式中的 −buv′。

检验函数可以看成检查方程的一把工具。未知函数 u 在端点可以非零；为零的是检验函数 v，不要混淆。

## 5. 积分怎样变成线性方程组

代入 $u_h=\sum_jU_j\phi_j$，依次选 $v=\phi_i$：

$$\sum_j A_{ij}U_j=F_i,$$
$$A_{ij}=\int a\phi_j'\phi_i'+\int b\phi_j'\phi_i+\int c\phi_j\phi_i,\quad F_i=\int f\phi_i.$$

三块矩阵相加：$A=K+B+M_c$。F 则来自 f。积分用积分点与权重近似，小单元上的矩阵累加成全局矩阵，叫组装。

以 a=1,b=c=0,f=2、两个单元 h=0.5 为例：每个单元矩阵为

$$K_e=\frac1h\begin{bmatrix}1&-1\\-1&1\end{bmatrix}=\begin{bmatrix}2&-2\\-2&2\end{bmatrix},$$

单元右端为 $[0.5,0.5]^T$。累加后，施加边界前为

$$\begin{bmatrix}2&-2&0\\-2&4&-2\\0&-2&2\end{bmatrix}\begin{bmatrix}U_0\\U_1\\U_2\end{bmatrix}=\begin{bmatrix}0.5\\1\\0.5\end{bmatrix}.$$

端点给定 U₀=U₂=0 后，只解内部行 $4U_1=1$，得到 0.25。边界两行由已知端点条件替代，不能把未处理的三行都当成最终未知方程。

非零边界还要将已知端点贡献移到右端：$A_{II}U_I=F_I-A_{ID}g_D$。代码中的 DirichletBC 同时处理矩阵和右端。

## 6. 按这个顺序读代码

| 函数 | 作用 |
| --- | --- |
| [manufactured_case（第35行）](./cdr_solver.py) | 定义 a、b、c、精确解和测试源项 |
| [expression_function（第24行）](./cdr_solver.py) | 将表达式转换成可求值函数 |
| [solve_cdr（第74行）](./cdr_solver.py) | 当前主入口：a、b、c、f、边界 |
| [_solve（第51行）](./cdr_solver.py) | 网格→组装→边界→稀疏求解 |
| [solve_transport（第66行）](./cdr_solver.py) | 守恒形式对照，初读可以跳过 |
| [equation_balance（第79行）](./cdr_solver.py) | 检查积分形式的全域收支误差 |
| [run（第103行）](./cdr_solver.py) | 32 组实验、数据与图片 |

关键流程：

```python
mesh = IntervalMesh.from_interval_domain(list(interval), nx=n)
space = LagrangeFESpace(mesh, p=p)
```

这两行创建小段网格和分段多项式空间。n 是单元数，p 是次数，q 是数值积分参数，三者不同。

```python
form.add_integrator(ScalarDiffusionIntegrator(coef=a, q=q))
form.add_integrator(ScalarMassIntegrator(coef=c, q=q))
convection.add_integrator(ScalarConvectionIntegrator(coef=b, q=q))
```

三项对应 a、c、b。主接口选 advective 分支，因此矩阵是 K+B+M_c；代码中的 −B.T 只服务保留的守恒对照接口。

```python
rhs.add_integrator(ScalarSourceIntegrator(source=f, q=q))
F = rhs.assembly()
```

**这就是 f 所在的位置。** 它不是系数矩阵的一项，而是右端向量的来源。

```python
A, F = DirichletBC(space, gd=gd, threshold=boundary).apply(A, F)
uh[:] = spsolve(A.copy(), F.copy(), solver='scipy')
```

先处理已知端点，再用稀疏求解器求未知数。稀疏是因为局部帽子函数通常只与附近帽子重叠。

expression_function 用 SymPy 的 lambdify 将数学表达式变成 NumPy 函数，并处理常数广播。FEALPy 即使在一维也需要速度输出最后带长度为 1 的向量轴，所以 b 使用 vector=True。

## 7. 为什么测试时又“算出 f”

真实求解：给定 a、b、c、f 和边界，求未知 u。

开发测试：先故意选一个标准答案 $u_*=\sin(\pi x)+1+x$，再反算

$$f=-(au_*')'+bu_*'+cu_*.$$

这样就有一份带答案的考卷。这叫制造解法，不代表所有实际问题的 f 都由精确解生成，更不是求解器不需要 f。

L2 误差测曲线整体偏差；H1 半范误差测斜率偏差。网格步长减半，误差缩小四倍就是二阶。光滑且稳定的当前算例中，P1 的两种阶接近 2/1，P2 接近 3/2。

代数残差小只说明 AU≈F，不能保证选对方程或没有伪振荡。强对流下本程序仍可能给出非物理负值，已经保留限制案例。

## 8. 开始运行

```powershell
.\run.cmd
```

克隆仓库后先按 README 建好虚拟环境与依赖，再执行上面的命令。

直接调用一题可以照着 example_source.py。它实际求解非零源项，不使用精确解产生输入源项。

先看 manufactured_case 的 variable，再看 solve_cdr 和 _solve；后者已委托 cdr_lfem_solver_1d.py 中的 CdrLFEMSolver1D 完成组装求解，最后读 run 的误差与画图。此处仅讲一维稳态、Dirichlet、P1/P2；二维、时间项、混合边界和稳定化尚未实现。

完整源程序和实测表格见 [任务报告](./一维求解器_任务报告.md)。
